import styled from "styled-components";

export const ButtonPrimary = styled.button`
  background: var(--accent);
  color: var(--main);
  padding: 10px 15px;
  font-weight: 400;
  border: 0;
  border-radius: 5px;
  font-size: 16px;
  letter-spacing: 1px;
`;
